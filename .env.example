# AquaFlow local secrets (not committed)

# Speech-to-text (Phase 8A). Server only — never put these in client code.
# Create an Azure Speech resource, then set:
AZURE_SPEECH_KEY=
AZURE_SPEECH_REGION=eastus
AZURE_SPEECH_LOCALE=en-GH
